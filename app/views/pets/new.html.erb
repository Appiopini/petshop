<div class="container">
  <div class="row justify-content-center">
    <div class="col-md-6">
      <%= link_to pets_path(@pet) do %>
        <div class="btn btn-warning btn-sm"> Todas as Contas <i class="fas fa-edit"></i>
        </div>
      <% end %>
      <h1>Cadastrar Conta</h1>
        <%= render 'form', pet: @pet %>
      <p> </p>
    </div>
  </div>
</div>
