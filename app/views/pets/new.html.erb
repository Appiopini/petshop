<div class="container">
  <div class="row justify-content-center">
    <div class="col-md-6">
      <%= link_to pets_path(@pet) do %>
        <div class="btn btn-warning"> Todos os Pets <i class="fas fa-edit"></i>
        </div>
      <% end %>
      <h1>Cadastrar Pet</h1>
        <%= render 'form', pet: @pet %>
      <p> </p>
    </div>
  </div>
</div>
